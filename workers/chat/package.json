{
  "name": "eastpole-chat",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that brokers East Pole pool chat → Claude API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
